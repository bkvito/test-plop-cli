# single-pick 模板微调思路文档

## 思路

- 本次任务重点是验证测试链路，所以改动需要足够小
- 选择根节点增加 `data-template-type` 标记，因为它不会影响现有变量替换逻辑
- 同步增加断言，确保测试真正覆盖本次模板变化

## 取舍

- 没有调整组件入参或 hook 逻辑，避免引入额外行为变化
- 没有扩大到其他模板类型，保持本次验证范围聚焦在 `single-pick`
