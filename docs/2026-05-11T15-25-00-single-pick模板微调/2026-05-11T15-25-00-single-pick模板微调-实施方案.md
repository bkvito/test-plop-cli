# single-pick 模板微调实施方案

## 目标

- 对 `single-pick` 模板做一次最小内容改动
- 确认模板验证脚本能覆盖到本次改动
- 执行全量模板验证，观察是否存在回归问题

## 实施步骤

1. 在 `single-pick` 的 setup 与 normal 模板根节点增加模板类型标记
2. 在自动校验脚本中补充对应关键片段断言
3. 执行全量模板验证命令

## 伪代码

```txt
修改 single-pick 根节点标记
生成全部模板组合
检查 single-pick 输出中是否包含新增标记
```
