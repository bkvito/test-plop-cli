# Vitest 与 CLI E2E 测试方案总结

## 本次完成内容

- 增加 `test` 与 `test:watch` 命令
- 增加 Vitest 开发依赖
- 新增生成函数集成测试
- 新增 CLI E2E 测试
- 更新 README，补充正式测试入口说明

## 当前收益

- 模板正文小幅变化时，不再需要频繁同步修改测试脚本
- 能覆盖全部模板组合是否正常生成
- 能覆盖 CLI 真实调用链路
- 已执行 `npm run test`，2 个测试文件、12 个用例全部通过

## 当前不足

- 尚未引入快照或 golden file 测试
- 旧的 `scripts/verify-templates.mjs` 仍然存在，可后续评估是否保留或降级为手动验收工具
- 首轮测试发现 Vue 合法插值 `{{ item }}` 不能按普通占位符残留处理，已调整为只检查脚手架占位符

## 后续方向

- 如果模板输出稳定，可以再引入快照测试
- 如果准备发布，可以补充 `npm pack` 冒烟测试
